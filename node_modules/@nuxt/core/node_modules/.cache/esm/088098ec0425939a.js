let merge;_042‍.x([["defaults",()=>defaults],["default",()=>initOptions]]);_042‍.w("deepmerge",[["default",["merge"],function(v){merge=v}]]);
       const defaults = {
    customVariables: [],
    defaultAssets: {
        font: {
            family: 'Roboto'
        },
        icons: 'mdi'
    },
    optionsPath: undefined,
    treeShake: process.env.NODE_ENV === 'production'
};
               function initOptions(moduleOptions) {
    const options = merge.all([
        defaults,
        this.options.vuetify || {},
        moduleOptions || {}
    ]);
    return options;
}
//# sourceMappingURL=options.js.map